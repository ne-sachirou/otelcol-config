---
receivers:
  hostmetrics:
    collection_interval: 1m
    scrapers:
      cpu:
      disk:
      filesystem:
      load:
      memory:
      network:
      paging:
      # process:
      #   mute_process_exe_error: true
      #   mute_process_io_error: true
      #   mute_process_name_error: true
      #   mute_process_user_error: true
      processes:
      system:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318
  otlp/c4se:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4319

processors:
  attributes/mypc:
    actions:
      - action: upsert
        key: service.namespace
        value: ne-sachirou
      - action: upsert
        key: service.name
        value: ne-sachirou-pc-macbook
  attributes/trace:
    actions:
      - action: upsert
        key: service.namespace
        value: debug
      - action: upsert
        key: service.name
        value: debug
  resourcedetection/system:
    detectors: ["system"]
    system:
      hostname_sources: ["os"]

exporters:
  mackerel_otlp:
  mackerel_otlp/c4se:
    mackerel_api_key: ${env:MACKEREL_APIKEY_C4SE}
  debug:
    verbosity: detailed

service:
  telemetry:
    metrics:
      level: detailed
      readers:
        - periodic:
            exporter:
              otlp:
                protocol: http/protobuf
                endpoint: http://127.0.0.1:4318/v1/metrics
  pipelines:
    metrics:
      receivers: [hostmetrics, otlp]
      processors: [attributes/mypc, resourcedetection/system]
      exporters: [mackerel_otlp]
    metrics/c4se:
      receivers: [otlp/c4se]
      processors: []
      exporters: [mackerel_otlp/c4se]
    traces:
      receivers: [otlp]
      processors: [attributes/trace]
      exporters: [debug]
