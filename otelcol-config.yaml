---
receivers:
  hostmetrics:
    collection_interval: 1m
    scrapers:
      cpu:
      disk:
      filesystem:
      load:
      memory:
      network:
      paging:
      # process:
      #   mute_process_exe_error: true
      #   mute_process_io_error: true
      #   mute_process_name_error: true
      #   mute_process_user_error: true
      processes:
      system:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

processors:
  # attributes/mypc:
  #   actions:
  resource/mypc:
    attributes:
      - action: upsert
        key: service.namespace
        value: ne-sachirou
      - action: insert
        key: service.name
        value: ne-sachirou-pc-macbook
  # attributes/trace:
  #   actions:
  resource/trace:
    attributes:
      - action: upsert
        key: service.namespace
        value: ne-sachirou
      - action: insert
        key: service.name
        value: ne-sachirou-pc-macbook
  resourcedetection/system:
    detectors: ["system"]
    system:
      hostname_sources: ["os"]

exporters:
  mackerel_otlp:
  debug:
    verbosity: detailed

service:
  telemetry:
    metrics:
      level: detailed
      readers:
        - periodic:
            exporter:
              otlp:
                protocol: http/protobuf
                endpoint: http://127.0.0.1:4318/v1/metrics
  pipelines:
    metrics:
      receivers: [hostmetrics, otlp]
      processors: [resource/mypc, resourcedetection/system]
      exporters: [debug, mackerel_otlp]
    traces:
      receivers: [otlp]
      processors: [resource/trace]
      exporters: [debug]
